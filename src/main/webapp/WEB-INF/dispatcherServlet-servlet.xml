<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. 开启组件扫描：扫描Controller所在的包 -->
    <context:component-scan base-package="onlineeducationplatform.controller"/>

    <!-- 2. 开启Spring MVC注解驱动（支持@RequestMapping等注解） -->
    <mvc:annotation-driven/>

    <!-- 3. 配置视图解析器（指定页面的前缀和后缀） -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/"/> <!-- 页面文件所在目录 -->
        <property name="suffix" value=".jsp"/> <!-- 页面文件的后缀 -->
    </bean>

</beans>